package controlSelenium;

import org.openqa.selenium.By;

public class Image extends Control{

    public Image(By locator, String myNameControl) {
        super(locator, myNameControl);
    }
}
